@using Webdiyer.WebControls.Mvc;
@model PagedList<EYB.ServiceEntity.BaseInfo.MainCardEntity>
@{
    ViewBag.Title = "选择储存卡类型";
    Layout = null;
}
<form  action="" method="post" id="searchForm"  >
<input type="hidden" value="" name="orderDirection" id="orderDirection" />
<input type="hidden" value="" name="orderField" id="orderField" />
<table class="gridTable" style="height: auto; border-collapse: collapse; margin: 0px;"
    cellpadding="0" cellspacing="0">
    <thead>
        <tr>
            <th>
                选择
            </th>
            <th>
                存储卡名称
            </th>
            <th>
                类别名称
            </th>
            <th>
                产品价格
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var entity in Model)
        {
            var listValue = entity.ID + "|" + entity.Name + "|" + entity.CardTypename + "|" + entity.Price+"$";
            <tr class="ableclick">
                <td>
                    <input name="radioList" type="checkbox" value="@listValue" />
                </td>
                <td>
                    @entity.Name
                </td>
                <td>
                   @entity.CardTypename
                </td>
                <td>
                    @entity.Price
                </td>
            </tr>
        }
    </tbody>
</table>
@Ajax.Pager(Model, new PagerOptions { PageIndexParameterName = "pageNum", PagerItemsSeperator = "", ShowPageIndexBox = false, CurrentPagerItemWrapperFormatString = "<span class=\"current\">{0}</span>" }, new MvcAjaxOptions { UpdateTargetId = "articles", HttpMethod = "Post", DataFormId = "searchForm" }, new { id = "badoopager" })
</form>

